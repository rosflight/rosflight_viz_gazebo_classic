# Gazebo Classic Visualizer

This repo has the files necessary to use the Gazebo Classic for ROSflight simulation.
This is accomplished by cloning this repository into the `rosflight_sim/simulators` directory of the `rosflight_ros_pkgs` repository.
The `colcon` will automatically build it if `ros-humble-desktop` is installed, or if Gazebo Classic was installed independently.
